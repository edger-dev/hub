[package]
name = "hub"
version = "0.0.0"
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
hub-protocol = { path = "../hub-protocol" }
vox = "0.10.0-rc.5"
tokio = { version = "1", features = ["sync", "net", "rt", "rt-multi-thread", "macros"] }

[dev-dependencies]
tempdir = "0.3"
facet-json = "0.50.0-rc.5"
node = { path = "../node" }
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros", "time"] }
