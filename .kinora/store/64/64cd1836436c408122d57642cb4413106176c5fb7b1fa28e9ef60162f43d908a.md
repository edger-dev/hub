# Plan — M2: the node daemon stub

**Task:** kino://fab72ddcdfba854a1330679380958961aeb445a93660bf8445dfdf6f6400e75f

Build `crates/node` inside-out: pure config + service impl first (no transport),
then the vox reverse-dial wiring, then the reconnect loop. TDD throughout.

## Phase 1 — config + stub service (pure, no transport)

- Create `crates/node` (lib), add to workspace `members`; drop
  `tests/architecture.rs`.
- `NodeConfig { node, link, hub, connectors, agent_version }`. Constructing it
  without a hub endpoint is a hard error (§2.4) — the type/validator enforces it.
- `StubNode`: `impl NodeService` — `ping` → `Pong { node, agent_version }`;
  `deliver` → `Err(NodeError::UnknownConnector)` (no connectors wired yet).
- `registration()` — build the `Registration` a link announces from config.
- Unit tests: hub-required error; ping payload; registration built from config.

*Status:* not started.

## Phase 2 — reverse-dial once (vox, memory-link tested)

- `connect_once(link)`: as vox initiator, serve `NodeServiceDispatcher::new(stub)`
  inbound and open a `NodeIngressClient` to call `register(registration())`.
- End-to-end test over `vox::memory_link_pair`: a fake hub accepts `NodeIngress`
  (records the `Registration`) and calls the node's `ping` back; assert the node
  registered and answered.

*Status:* not started.

## Phase 3 — reconnect forever (bounded backoff)

- `run(connect)`: loop — dial, on success serve+register and await disconnect,
  then back off (bounded) and re-dial; forever (§2.1). Needs tokio timers.
- Test: after the fake hub drops the connection, the node re-dials and
  re-registers.

*Status:* not started.

## Phase 4 — lint-clean, review, link intent

- Zero warnings; `cargo test --workspace` green; review the diff.
- Add `// implements:` refs (hub-reverse-dial-reconnect,
  hub-uniform-node-addressing). Mark task + plan resolved.

*Status:* not started.
