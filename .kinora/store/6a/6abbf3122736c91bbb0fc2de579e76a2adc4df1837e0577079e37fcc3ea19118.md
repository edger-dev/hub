# Task — M3: the hub registry, discovery, and routing

**Goal.** `crates/hub` — the hub behind the M1 contract. It accepts
reverse-dialed nodes (`NodeIngress::register`), maintains the live registry,
serves discovery (`topology` + `subscribe_topology`), and routes consumer calls
to the selected link (`route`). Success is the **real `node` crate talking to the
real hub** over vox, replacing the fake hub from M2.

**Realizes:**

- kino://7c924d1c2994326c261f907f81c39f0fa676381b53f7898556893bb4f750d85c —
  live node discovery from the reverse-dial registry (§2.6).
- kino://08c3ce357742ccf578caddcfb6578a92216b515adcfe1ac9de4543406be52cb0 —
  one node, many links; route defaults to and falls back on `stable` (§2.5).
- kino://ba47c87abc0f961967d5ccfa9254945075c16c1e9a3b8eaed1e8195e1663c241 —
  the audience-split services (`NodeIngress` / `ConsumerApi`).

**Plan:** kino://PLAN_ID_PLACEHOLDER

**Done when:**

- `crates/hub` builds under the workspace lints with **zero warnings**.
- Registry: keyed `(node, link)`; re-registration by a known pair is accepted
  churn, not an error; a node appears on its first link and disappears with its
  last (§2.6).
- Routing honours the spec: target link, else `stable`, else `UnknownLink`;
  `UnknownNode` when nothing is connected (§2.5).
- Discovery: `topology` snapshot + a `subscribe_topology` tail that reports
  node/link connect and disconnect.
- End-to-end: the real `node` crate reverse-dials the real hub, registers,
  appears in discovery, and answers a consumer's routed call.
- `tests/architecture.rs` present; committed; task + plan resolved.

**Out of scope:** the event store (§2.7), the console (§3–4), service packaging
and deploy (§2.3, §5), auth/ACLs, production transport wiring beyond what the
end-to-end test needs.
