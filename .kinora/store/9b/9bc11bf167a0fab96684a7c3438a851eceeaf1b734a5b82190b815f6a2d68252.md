# Plan — M1: the hub protocol API surface

**Task:** kino://c6178132314d68a977b50be334976aa0ab5a22547331573f4dea0ef61027048b

Build the `crates/hub-protocol` contract bottom-up: identity → messages →
services. TDD throughout — facet round-trips are the natural first failing tests.

## Phase 1 — scaffold & de-risk the stack

- Create `crates/hub-protocol` (lib), add to workspace `members`.
- Add deps: `facet` (derive), `vox`. Confirm the crate builds green in the nix
  devShell — this de-risks the vox/facet rc toolchain before any real design.
- Drop `tests/architecture.rs` (the jig `prefer-file-modules` mechanism).

*Status:* not started.

## Phase 2 — facet message + identity types (TDD)

Types the brief implies, smallest set that carries M1:

- **Identity:** `NodeId` (registry name — §2.4), `LinkLabel` (`stable`/`dev` +
  custom — §2.5), `NodeLink` (node + link).
- **Registration:** what a reverse-dialing link announces — node id, link label,
  offered connectors, agent/version metadata (§2.1, §2.4).
- **Connector:** a descriptor identifying a connector a link offers (process mgmt,
  sessions, files, commands — §1). Kind + id, not the impl.
- **Discovery:** `NodeInfo` / `TopologySnapshot` — live connected nodes, their
  links and connectors (§2.6).
- **Targeting:** `LinkSelector` — node + optional link label, encoding
  default-to-stable / fall-back-to-stable routing (§2.5).

Tests first: facet serialize → deserialize round-trip + shape assertions.

*Status:* not started.

## Phase 3 — vox service traits

- `HubService` — dialed by both nodes and consumers: `register`, topology
  `snapshot` + live `tail`, open a connector lane to a `LinkSelector`, route a
  call. (Exact method set to firm up against the vox rc API.)
- Node-facing service (hub → node): `ping`/health + `receive` a routed call.
- Compile only; no impls.

*Status:* not started.

## Phase 4 — lint-clean, arch test green, review

- Zero warnings (`.bacon-claude-diagnostics`); `cargo test --workspace` green.
- Code-review the surface (fresh subagent) — naming, faithfulness to the brief,
  facet/vox idiom. Fix in a separate commit.
- Carve the atomic specs actually pinned down by the wire types (e.g.
  `hub-node-identity`, `hub-one-node-many-links`) from the brief and add
  `// implements:` refs. Mark task + plan resolved.

*Status:* not started.
